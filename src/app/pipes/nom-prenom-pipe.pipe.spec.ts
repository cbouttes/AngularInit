import { NomPrenomPipePipe } from './nom-prenom-pipe.pipe';

describe('NomPrenomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NomPrenomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
