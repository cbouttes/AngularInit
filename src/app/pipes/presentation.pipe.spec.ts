import { PresentationPipe } from './presentation.pipe';

describe('PresentationPipe', () => {
  it('create an instance', () => {
    const pipe = new PresentationPipe();
    expect(pipe).toBeTruthy();
  });
});
